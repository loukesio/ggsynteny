% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_circular_microsynteny.R
\name{plot_circular_microsynteny}
\alias{plot_circular_microsynteny}
\title{Plot gene-level synteny around a circle}
\usage{
plot_circular_microsynteny(
  features,
  links,
  bin_order = NULL,
  palette = NULL,
  gene_fill = "per_name",
  gene_palette = NULL,
  gene_color = "#333333",
  gene_alpha = 0.95,
  ribbon_fill = "identity",
  ribbon_palette = NULL,
  ribbon_alpha = 0.35,
  ribbon_anchor = "body",
  identity_low = "#DCEEFF",
  identity_high = "#08519C",
  gap = 2,
  group_gap = 10,
  start_angle = 90,
  clockwise = TRUE,
  curvature = 0.65,
  track_width = 0.065,
  arrowhead_frac = 0.18,
  label_genes = TRUE,
  label_size = 2.5,
  bin_label_size = 4,
  interactive = FALSE,
  title = NULL
)
}
\arguments{
\item{features}{Data frame with \code{bin_id}, \code{seq_id}, \code{start}, \code{end}, \code{strand}
(\code{"+"} or \code{"-"}), unique \code{feat_id}, and \code{name} columns.}

\item{links}{Data frame with \code{feat_id_a}, \code{feat_id_b}, and optional percent
\code{identity} (0 to 100; \code{NA} means unknown). Empty links are supported.}

\item{bin_order}{Bins in circular display order. Defaults to first appearance
in \code{features}; may select a subset. Links to excluded bins are omitted.}

\item{palette}{Built-in ltc name, HCL palette name, \code{"Okabe-Ito"}, or color
vector for genes and categorical ribbons. Identity keeps the default blue
ramp unless \code{ribbon_palette} is supplied explicitly.}

\item{gene_fill}{Gene coloring: \code{"per_name"}, \code{"per_feat"}, or \code{"uniform"}.}

\item{gene_palette}{Overrides \code{palette}; named vectors map gene names or IDs.}

\item{gene_color}{Gene outline color.}

\item{gene_alpha}{Gene opacity, between 0 and 1.}

\item{ribbon_fill}{Ribbon coloring: \code{"identity"}, \code{"per_name"}, or \code{"uniform"}.}

\item{ribbon_palette}{Overrides the categorical palette or sets an identity
color ramp. Named mappings are supported for per-name ribbons.}

\item{ribbon_alpha}{Ribbon opacity, between 0 and 1.}

\item{ribbon_anchor}{\code{"body"} keeps arrowheads clear; \code{"full"} spans the
complete gene interval.}

\item{identity_low, identity_high}{Colors at 0 and 100 percent identity when
no explicit ribbon palette is supplied.}

\item{gap}{Gap between contigs within a bin, in degrees.}

\item{group_gap}{Gap between bins, in degrees.}

\item{start_angle}{First sector's starting angle in degrees; 90 is top.}

\item{clockwise}{Draw increasing genomic coordinates clockwise?}

\item{curvature}{Pull of ribbon control points toward the center (0 to 1).}

\item{track_width}{Radial thickness of the gene arrows; outer radius is 1.}

\item{arrowhead_frac}{Fraction of gene length used for the arrowhead (0 to
0.5), capped at 6 degrees to keep very long arrows readable.}

\item{label_genes}{Show gene names outside the ring?}

\item{label_size}{Gene label size in mm.}

\item{bin_label_size}{Bin label size in mm. Set to 0 to hide labels.}

\item{interactive}{Build ggiraph layers for rendering with \code{\link[=syn_girafe]{syn_girafe()}}?}

\item{title}{Optional title.}
}
\value{
A ggplot object whose \code{data} contains the contig layout. Attributes
\code{circular_features} and \code{circular_links} contain displayed features and links
with angular coordinates in radians.
}
\description{
Draw strand-aware curved gene arrows on contig arcs, grouped by genome/bin,
with homology ribbons inside the circle. Uses ordinary ggplot2 layers and
the same feature/link tables as \code{\link[=plot_microsynteny]{plot_microsynteny()}}.
}
\details{
Each contig spans its first feature start through its last feature
end; unannotated flanks are not inferred. Arc widths are proportional to
these observed spans across bins, using a shared coordinate unit. The
circular presentation does not imply biologically circular sequences.
Features that cross a circular genome origin must be split before plotting.

Gene strand controls arrow direction. Ribbons represent homology between
intervals, not alignment orientation; no inversion is inferred from strand.
Links can connect any displayed bins or contigs, including the same bin.
Unknown feature identifiers, duplicate feature IDs and invalid intervals
are rejected to avoid ambiguous links.
}
\examples{
micro <- demo_microsynteny_data()
p <- plot_circular_microsynteny(micro$features, micro$links,
                               palette = "casa_natal")
p + ggplot2::labs(caption = "Gene arrows indicate strand")
}
