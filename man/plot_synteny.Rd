% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_synteny.R
\name{plot_synteny}
\alias{plot_synteny}
\title{Plot macro-synteny across multiple species}
\usage{
plot_synteny(
  syn_data,
  species_order,
  palette = NULL,
  tier_spacing = 18,
  chr_fill = "per_species",
  chr_palette = NULL,
  ribbon_fill = "source_chr",
  ribbon_palette = NULL,
  ribbon_alpha = 0.3,
  curvature = 0.55,
  chr_radius = 0,
  label_size = 2.5,
  species_label_size = 4.5,
  interactive = FALSE,
  title = NULL
)
}
\arguments{
\item{syn_data}{List containing \code{chromosomes} and \code{blocks} data frames.
See \code{\link{example_synteny_data}} for format.}

\item{species_order}{Character vector specifying species display order (top to bottom)}

\item{palette}{A palette for the whole plot: the name of a built-in palette
(all 32 ltc palettes, e.g. \code{"casa_natal"} or \code{"minou"}; list them with
\code{\link{syn_palettes}}), \code{"Okabe-Ito"}, or a vector of colors. Used
for both chromosomes and ribbons unless \code{chr_palette} / \code{ribbon_palette}
override it.}

\item{tier_spacing}{Numeric, vertical spacing between species tiers (default 18)}

\item{chr_fill}{Chromosome coloring mode: "per_species", "uniform", "per_chr", or "custom"}

\item{chr_palette}{Color palette for chromosomes: a built-in palette name,
an unnamed color vector, or a named vector mapping keys to colors (see Details)}

\item{ribbon_fill}{Ribbon coloring mode: "source_chr", "target_chr", "species_pair", "uniform", or "custom"}

\item{ribbon_palette}{Color palette for ribbons: a built-in palette name,
an unnamed color vector, or a named vector mapping keys to colors (see Details)}

\item{ribbon_alpha}{Ribbon transparency (0-1, default 0.30)}

\item{curvature}{Ribbon curve strength (0-1, default 0.55)}

\item{chr_radius}{Corner radius of the chromosome boxes in millimetres
(default 0 = square corners). Try 1-2 mm for gently rounded
chromosomes; rounding uses \pkg{ggforce}, so it looks right at any
figure size. Not available together with \code{interactive = TRUE}
(square corners are drawn instead, with a warning).}

\item{label_size}{Chromosome label size (default 2.5)}

\item{species_label_size}{Species label size (default 4.5)}

\item{interactive}{Logical; make chromosomes and ribbons interactive
(hover highlight and tooltips) using \pkg{ggiraph}? Render the result
with \code{\link[=syn_girafe]{syn_girafe()}}. Default \code{FALSE}.}

\item{title}{Optional plot title}
}
\value{
A ggplot2 object (pass to \code{\link[=syn_girafe]{syn_girafe()}} to render an interactive
widget when \code{interactive = TRUE})
}
\description{
Creates publication-quality synteny plots showing chromosomal relationships
across multiple species with flexible coloring options.
}
\details{
\strong{Chromosome Coloring (chr_fill):}
\itemize{
\item "per_species" — one color per species. chr_palette = "casa_natal" or c("Human" = "#4477AA", ...)
\item "uniform" — all chromosomes same color. chr_palette = "#E8E4DF"
\item "per_chr" — one color per chromosome label. chr_palette = "casa_natal" or c("1" = "#4477AA", ...)
\item "custom" — full control. chr_palette as named vector with "species__chr" keys
}

\strong{Ribbon Coloring (ribbon_fill):}
\itemize{
\item "source_chr" — colored by the source (top) chromosome
\item "target_chr" — colored by the target (bottom) chromosome
\item "species_pair" — one color per tier gap. ribbon_palette = c("At_Vv" = "#3A6EA5", ...)
\item "uniform" — all same color. ribbon_palette = "#6688AA"
\item "custom" — per-block. ribbon_palette = vector of colors
}

Wherever a palette is expected, a single unnamed string is first matched
against the built-in palette names (case-insensitively, ignoring spaces,
underscores and dashes) — so \code{"casa_natal"}, \code{"Casa Natal"} and
\code{"casanatal"} all find the same palette. Named vectors are used as
explicit key-to-color mappings, exactly as before.
}
\examples{
# Load example data
syn <- example_synteny_data()
sp_order <- c("Arabidopsis", "Grape", "Rice")

# Default: per-species chromosomes + source-chr ribbons
p <- plot_synteny(syn, sp_order)

# One ltc palette for the whole plot
p <- plot_synteny(syn, sp_order, palette = "casa_natal")

# Custom colors
p <- plot_synteny(syn, sp_order,
                  chr_fill = "uniform",
                  chr_palette = "#E8E4DF",
                  ribbon_fill = "species_pair",
                  ribbon_palette = c("Arabidopsis_Grape" = "#3A6EA5",
                                     "Grape_Rice" = "#E8573A"))

}
