% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gc_content.R
\name{gc_content}
\alias{gc_content}
\title{Calculate GC content from DNA sequences}
\usage{
gc_content(sequences, intervals = NULL, window = 1000, step = window)
}
\arguments{
\item{sequences}{Data frame with \code{group}, \code{seq_id}, and \code{sequence} (character
DNA strings, or \code{NA} for missing sequence). \code{species}/\code{chr} and
\code{bin_id}/\code{seq_id} key pairs are also accepted. Keys must be unique.
Sequences start at genomic coordinate zero. Lowercase is accepted;
whitespace is removed. Gaps and non-IUPAC DNA characters are rejected.}

\item{intervals}{Optional data frame with the same keys and \code{start}, \code{end}.
Uses zero-based, half-open base-pair coordinates: \verb{[0, 4)} includes the
first four bases. For per-gene summaries, pass the feature table after
converting its coordinates if necessary. Extra columns are retained.}

\item{window}{Positive integer window width in bases, used only without
\code{intervals}. The last window is shortened at the end of a sequence.}

\item{step}{Positive integer distance between window starts. Defaults to
\code{window} (non-overlapping windows); smaller steps give sliding windows.}
}
\value{
Data frame with \code{group}, \code{seq_id}, \code{start}, \code{end}, \code{value} (GC percent),
\code{n_called} (A/C/G/T count), and \code{n_ambiguous} (other IUPAC base count).
For missing sequences both counts are \code{NA}. Supplied interval columns
are retained, except these result columns which are replaced. Output can
be passed directly to \code{\link[=syn_track]{syn_track()}} when plot units and origins match.
}
\description{
Calculate \code{100 * (G + C) / (A + C + G + T)} for gene intervals or genomic
windows. Ambiguous IUPAC DNA bases are excluded from the denominator. No
called bases, or an explicitly missing sequence, gives \code{NA}, never zero.
}
\details{
No sequence is inferred from synteny links or block ranks. Missing
sequence keys are errors; declare a sequence as \code{NA} to explicitly mark it
missing. Without intervals, missing sequences are rejected because their
lengths are unknown. Empty sequences produce no windows. Windows stop at
the sequence end and do not wrap across a circular origin. Split intervals
that cross the origin before calculating. Gene strand does not affect GC.
}
\examples{
dna <- data.frame(group = "Genome A", seq_id = "chr1",
                  sequence = "ACGTGGCCNNAT")
gc_content(dna, window = 4)
genes <- data.frame(group = "Genome A", seq_id = "chr1",
                    start = c(0, 8), end = c(8, 12))
gc_content(dna, intervals = genes)
}
